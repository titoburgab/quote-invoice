# Update the PDF Template

Purpose: steps for changing the branded HTML/PDF template (logo, layout, terms language)
without breaking the Claude JSON → PDF merge.

TBD — fill in once `tools/pdf/templates/` has a working template.
